# Selenium UI Header Test

Automated check of a website header — menu items and language switcher —
with **Selenium WebDriver (Firefox)**.

## Files
- `test_header_ui.py` — test scenario
- `requirements.txt` — dependencies

## Run
```bash
pip install -r requirements.txt
pytest -v
```
Requires Firefox + geckodriver in `PATH`.
